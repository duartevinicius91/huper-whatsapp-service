# Usar imagem base do Node.js
FROM node:18-slim

# Instalar dependências do sistema necessárias para Puppeteer e Chrome
RUN apt-get update && apt-get install -y \
    wget \
    gnupg \
    ca-certificates \
    fonts-liberation \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libc6 \
    libcairo2 \
    libcups2 \
    libdbus-1-3 \
    libexpat1 \
    libfontconfig1 \
    libgbm1 \
    libgcc1 \
    libglib2.0-0 \
    libgtk-3-0 \
    libnspr4 \
    libnss3 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libstdc++6 \
    libx11-6 \
    libx11-xcb1 \
    libxcb1 \
    libxcomposite1 \
    libxcursor1 \
    libxdamage1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxrandr2 \
    libxrender1 \
    libxss1 \
    libxtst6 \
    lsb-release \
    xdg-utils \
    && rm -rf /var/lib/apt/lists/*

# Criar diretório da aplicação
WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./

# Instalar dependências
RUN npm ci --only=production

# Copiar código da aplicação
COPY . .

# Criar diretório para autenticações locais
RUN mkdir -p auth

# Definir variáveis de ambiente padrão
ENV NODE_ENV=production
ENV PORT=3000
ENV AWS_REGION=us-east-1
ENV AWS_S3_REMOTE_DATA_PATH=whatsapp-sessions/
ENV DEBUG=false
ENV LOG_LEVEL=info

# Expor porta da aplicação
EXPOSE 3000

# Comando para iniciar a aplicação
CMD ["node", "api.js"]

